[繁體中文](TUTORIAL.md) | [English](TUTORIAL_en.md)

***

# MD AstellarTool 工具操作與技術使用手冊

本手冊為《MD AstellarTool》的完整操作指引。手冊將詳細說明工具運行所需的檔案環境、圖片命名規範、介面設定，以及各個獨立功能模組的運作流程與底層邏輯。本說明書旨在為一般使用者提供明確的操作步驟，同時為進階開發者揭示本工具底層的技術架構與安全機制。

---

## 一、 系統前置需求與圖片命名規範

在開始使用本工具進行任何修改前，請確認您的電腦環境已準備好以下目錄與檔案，並嚴格遵守圖片命名規則。

### 1. 必備的遊戲與系統路徑
* **遊戲 0000 檔案目錄**：
  遊戲本體的卡片與主要資產存放位置，路徑格式通常為：
  `[您的磁碟區]:\SteamLibrary\steamapps\common\Yu-Gi-Oh! Master Duel\LocalData\[8位數玩家ID]\0000\`
* **遊戲 StreamingAssets 目錄**（部分大檔資產）：
  `[您的磁碟區]:\SteamLibrary\steamapps\common\Yu-Gi-Oh! Master Duel\masterduel_Data\StreamingAssets\AssetBundle\`
* **遊戲大廳背景數據檔**（大廳背景修改專用）：
  `[您的磁碟區]:\SteamLibrary\steamapps\common\Yu-Gi-Oh! Master Duel\masterduel_Data\data.unity3d`

### 2. 圖片檔名命名規則與萃取機制
工具的自動化批次替換引擎是依據「檔案名稱前綴」來識別對應的卡片或資產 ID。

* **標準卡圖命名格式**：`[卡片ID]_[任意自訂文字].png` 或 `[卡片ID].png`
  * 正例：`20570_赫之聖女 卡爾塔西亞.png`、`4364.png`、`Alt17069_123.jpg`
  * 反例：`赫之聖女 卡爾塔西亞_20570.png`（無法識別，前綴非數字）。
  * [技術細節] 系統底層使用正規表達式 `^([a-zA-Z]*)(\d+)(.*)$` 進行精準萃取。引擎會自動過濾開頭的英文字母，直接鎖定連續的數字區塊作為卡片 ID，並將後方的字串視為標籤，藉此降低玩家命名失誤的機率。
* **非卡片視覺資產命名格式**：
  若為卡套、頭像、場地等物件，檔名必須保持與對照表中的名稱完全一致。
  * 範例：`ProfileIcon1020001.png`、`Mat_001_BaseColor.png`

---

## 二、 設定與外觀 (系統設定首站)

本模組負責管理全域環境變數、跨分頁路徑同步、多進程算力調度、介面主題外觀，以及多語系翻譯系統。建議在初次使用時優先設定此頁面。

<img width="1095" height="847" alt="設定" src="https://github.com/user-attachments/assets/c87beb83-5276-48bf-b9c5-3f5c7a3ef661" />

### 1. 介面與全域行為設定
* **寫入 JSON 設定檔 (儲存所有變更)**：
  點擊此按鈕會將目前介面上所有的輸入框路徑、勾選項狀態、色彩設定與自訂資料夾名稱，一次性存入 `md_tool_config.json` 檔案中。
  * [技術細節] 底層實作了防汙染儲存機制 (`save_single_key`) 與巨量快取剔除 (`.pop`)。當玩家在其他分頁載入上萬行卡片 ID 時，這些巨大的暫存草稿絕對不會被寫入 JSON 污染設定檔。此設計大幅降低了解析出錯的機率，並保護固態硬碟 (SSD) 避免無謂的巨量讀寫耗損。
* **跨分頁路徑同步**：
  啟用後，當您在任意頁面變更「專案根目錄」、「0000 原檔來源」或「CSV 對照表」路徑時，其他所有相關模組的對應輸入框皆會同步更新。
* **語系選擇與動態擴充**：
  設定 UI 顯示語言。下拉選單支援使用者自行手動輸入語言代碼。只要在 `MD_Tool_Essential/Languages/` 資料夾內放置對應的 JSON 翻譯檔，系統即可自動讀取。若發生部分 Key 缺失，系統具備安全降級 (Fallback) 機制，會自動顯示預設繁體中文以避免程式崩潰。
* **匯出待翻譯樣板**：
  點擊後，工具會啟動 Python `ast` 抽象語法樹解析器，走訪主程式原始碼，自動尋找所有被標記的字串與 UI 佔位符，並在 `Languages/` 目錄生成空白翻譯樣板 `raw.json`，同時安全更新 `zh-tw.json`。
* **最大算力 (執行緒/進程數)**：
  設定多進程並行運算的核心數。設為 `Auto` 時，程式會自動根據電腦 CPU 總核心數進行最佳化分配。

### 2. 外觀與互動體驗設定
* **五維度調色盤**：
  提供「強調色」、「背景色」、「文字色」、「側邊欄背景色」與「邊框色」五個獨立選擇器，支援 Alpha 通道透明度調配，即時套用動態 QSS 樣式表。
* **全域專案資料夾命名設定**：
  可在此統一設定預設的工作資料夾名稱（如原檔、原卡圖、文件備份、卡圖改等），系統底層在進行封裝與讀取時皆會嚴格參照此處設定。
* **側邊欄分頁排序與重命名**：
  可透過拖曳或使用上下按鈕調整左側目錄的順序，並可選取項目後修改顯示名稱。
* **自訂背景與九宮格錨點**：
  可選擇本機圖片作為 UI 背景圖。滑桿可調節背景透明度 (0\~100%) 與亮度 (10\~200%)。提供九宮格選取器（西北、北、東南等），決定背景圖片在視窗縮放時的對齊錨點。

---

## 三、 新手指南與操作旅程

工具將資產修改劃分為兩種主流模式，您可以依照當下需求選擇對應的模組：
* **標準專案流水線**：適合大型模組或多檔案變更。採取「爆搜對照表 -> 找出檔案 -> 提取圖片 -> 圖檔修改 -> 寫回 Bundle -> 打包發布」的結構。
* **快捷與自動化管線**：適合單卡快速替換、大廳背景修改、一鍵全自動超框，以及模組救援與虛擬掛載。

---

## 四、 爆搜與補齊 (資料庫建立與多語系擴充)

本模組為整個工具的資料核心，負責掃描遊戲原檔二進位數據，解密文字，並建立檔案 Hash 碼與卡片內容的結構化 CSV 對照表。

<img width="1093" height="844" alt="爆搜" src="https://github.com/user-attachments/assets/9b6cfa84-eaf0-4183-8976-28b62c36321a" />

### 1. 基礎對照表生成 (全盤爆搜)
* **執行方式**：填寫目標 0000 資料夾與輸出根目錄，設定所需語系，點擊「開始掃描」。
* **例外卡片處理**：
  [注意] 掃描完成後，若發現無法辨識類別的特殊卡片，系統會自動在輸出目錄生成 `Unknown_Cards_Report.json` 與 `Exception_IDs_List.txt`。系統不會因此崩潰，而是將這些卡片歸類為「例外」並預設套用常規相框，使用者可藉由該清單進行手動檢視。
* **記憶體防護機制**：
  [技術細節] 掃描過程中，系統透過 Windows `GlobalMemoryStatusEx` API 監控實體記憶體。當偵測到 RAM 使用率超過 85% 時，多進程池會自動進入動態休眠等待資源釋放，確保您的電腦絕對不會因為爆搜而死機。
* **XOR 混淆解密與自動猜解**：
  [技術細節] 遊戲資料庫採用動態 XOR 混淆加密。若遊戲更新導致預設密鑰失效，系統會啟動 `auto_crack_key` 暴力猜解引擎，在 0 到 1023 的範圍內逐一使用金鑰進行位元反轉並嘗試 zlib 解壓縮。只要解壓成功不報錯，系統就會自動捕獲新密鑰並更新設定檔，實現永久免疫官方加密更新。
* **IL2CPP Metadata 逆向映射**：
  [技術細節] 非卡片資產（如頭像、場地）僅有 Hash 命名。本工具會讀取 `global-metadata.dat`，提取 ID 字串並計算 CRC32 哈希，與 `ids_item.bytes` 進行雙向碰撞比對，精準還原出中文名稱。
* **TCG/OCG 雙卡面自動辨識**：
  [技術細節] 掃描時，工具會自動解析 Unity Container 路徑。若路徑包含 `/tcg/` 或 `/ocg/`，系統會自動在 CSV 對照表的 Container 欄位進行標註，方便進階玩家分類和諧版與未和諧版的異圖卡面。

### 2. 多語系橫向擴充
當您在遊戲中切換語言並下載新文字包後，可將舊有 CSV 貼入「待擴充的 CSV」欄位，修改語系代碼，點擊「執行補齊 / 擴充」。工具會比對新舊資料，在 CSV 右側自動新增新語言的名稱與效果欄位，無須重新掃描整個資料庫。

---

## 五、 找出檔案 (檔案定位與視覺化篩選)

依據 CSV 對照表，從龐大的 0000 目錄中精準尋找並複製出指定的目標 Hash 檔案。

<img width="1096" height="842" alt="找出" src="https://github.com/user-attachments/assets/3f22366e-02d1-465c-a389-159fdeea75b0" />

### 1. 操作模式選單
* **常規提取模式**：僅將目標卡片的 Hash 檔案從遊戲目錄複製到專案工作區。
* **快捷超框切換模式**：一鍵自動化模式。加入 ID 後點擊執行，系統會依序完成「複製原檔 -> 提取圖片 -> 11:16 超框裁切與 PSD 疊加 -> 寫回 Bundle -> 跳轉至超框註冊器」的全套作業。

### 2. 搜尋與視覺化沙盒篩選
* **搜尋引擎**：支援 Unicode 正規化與模糊搜尋。透過雙模式語系搜尋，即使輸入發生輕微拼寫錯誤，仍能算出相似度並列出結果。雙擊清單項目即可將其加入下方提取列表。
* **視覺化篩選 (沙盒模式)**：
  點擊「視覺化篩選」，系統會將提取清單中的 ID 傳送至圖形化瀏覽器的專屬沙盒中。使用者可在網格列表中檢視卡圖，選取不需要的卡片後按 `Delete` 刪除。點擊「確認修改並返回」，系統會將篩選後的名單覆寫回提取清單。

---

## 六、 提取資料 (Texture2D 導出與保護機制)

將複製出來的二進位 Bundle 檔案進行解包，導出 2D 貼圖資源。

<img width="1076" height="842" alt="提取" src="https://github.com/user-attachments/assets/91e4e574-9103-4825-9c8a-2a18666cf79e" />

### 1. 高畫質面積比對保護機制
[說明] 若玩家針對同一張卡片重複執行提取，無需擔心高畫質修改圖被官方原始低畫質圖片覆蓋。
[技術細節] 工具底層在寫入圖片前，會比對新舊圖片的總像素面積 `(w * h) <= (ext_img.w * ext_img.h)`。只有在新解析度大於或等於舊圖時才會執行覆寫，徹底保護您的高畫質卡圖。

### 2. 智慧雜訊過濾器 (`is_valuable_texture`)
[技術細節] 遊戲 Bundle 內充斥著大量的 3D 渲染貼圖（法線貼圖 Normal、金屬度 Metallic、粗糙度 Roughness 等）。本工具內建了精準的黑白名單過濾引擎。當讀取到無用 Shader 材質時會直接捨棄；而只要檔名符合視覺資產特徵，便予以保留，確保提取出的檔案夾乾淨無雜訊。

### 3. 輸出控制
支援同時匯出專屬 CSV 子對照表、圖片，以及保留原始 Bundle 備份（供後續還原或比較使用）。

---

## 七、 更改卡圖 (Bundle 二進位替換)

將外部修改好的新圖片，重新縫合寫回二進位 Bundle 檔案中。

<img width="1093" height="845" alt="改圖" src="https://github.com/user-attachments/assets/da18dfc5-efeb-470a-904e-b9bb1584c97b" />

### 1. 執行邏輯與視覺分類保護
填寫字典與專案根目錄後，點擊「開始替換」。程式會將「卡圖改」資料夾中的圖片寫入到備份檔中，並輸出至「改完的文件」。

[技術細節] 工具並非單純的二進位覆蓋。內部建有 `is_visual_asset` 分類引擎。若替換的是「卡片本體」，引擎會保持新圖片的原始解析度（保護高畫質 HD 卡圖）；若替換的是「卡套、大廳背景、頭像框」等 UI 介面，引擎會觸發 `MODE_FIT_VISUAL` 策略，強制使用影像處理界最高級的 Lanczos 重採樣演算法，將圖片無損縮放至與遊戲原廠完全吻合的尺寸，防止 UI 破圖。

---

## 八、 封裝模組 (目錄重構與發布打包)

將修改完成的 Bundle 檔案，自動組裝為遊戲官方要求的目錄結構。

<img width="1089" height="843" alt="打包" src="https://github.com/user-attachments/assets/3fa4427a-a506-4be0-b216-30cd440d5573" />

工具會讀取 CSV 對照表，判定每個檔案屬於常規 `0000` 還是 `StreamingAssets`。系統會在輸出資料夾中精準建立 `0000/xx/`（按 Hash 前兩位切分）資料夾，並支援一鍵壓縮為 ZIP 檔與附加 ReadMe 說明文件，便於後續直接覆蓋至遊戲目錄或進行發布。

---

## 九、 智慧串連執行 (一鍵自動化流水線)

提供工廠排程功能，可將「找出檔案、提取資料、靈擺後處理、更改卡圖、封裝模組」這五個步驟串接。依賴非同步回呼佇列 (Callback Queue)，只要確保各分頁的輸入路徑皆已填寫正確，即可一鍵無人值守完成所有流程。

<img width="1097" height="847" alt="串聯" src="https://github.com/user-attachments/assets/e7c4eb7d-f103-42f6-bb21-b08d96276016" />

---

## 十、 靈擺與超框卡圖後處理 (三大模式全解析)

專為卡片變形修復與 11:16 比例超框立體卡圖 (Over-Frame Art) 設計的影像處理中樞。

<img width="1095" height="853" alt="後處理" src="https://github.com/user-attachments/assets/59a8b6b5-fd86-4b94-ab31-a6a1a9eb0c65" />

### 1. 靈擺卡圖後處理 (卡圖改)
遊戲內的靈擺卡圖框長寬比與一般卡片不同。設定「靈擺填充比例」後，程式會在圖片下方補上對應比例的完全透明背景，增加圖片總高度，防止遊戲內渲染拉伸。

[技術細節] 靈擺判定極度困難，因此底層具備「混血雙軌判定引擎」。第一防線提取二進位中的 6-bit 物理遮罩比對特徵碼；第二防線使用 NLP 語意判讀掃描效果文尋找關鍵字。兩道防線聯集，徹底解決靈擺卡片分類遺漏的問題。

### 2. 超框卡圖後處理 (卡圖改)
超框卡圖需要 11:16 的比例。
[技術細節] 工具整合了 `psd-tools` 解析引擎與 `_psd_cache` 記憶體快取。它會將 `CardFrame/` 目錄下的 PSD 檔解構為五個獨立的 Alpha 遮罩圖層。當您調整透明度時，引擎會在記憶體中重新運算 Alpha 通道，並依照嚴格的 Z 軸順序 (`底圖 -> EffBox -> EffFrame -> ArtFrame -> NameBox -> PeriFrame`) 與經過 11:16 置中裁切的圖片進行 `Image.alpha_composite` Alpha 疊加，達成媲美 Photoshop 的合成效果。

### 3. 靈擺原卡圖處理 (原卡圖)
針對從遊戲中提取出來的原始靈擺卡圖進行尺寸校正。
[技術細節] 官方的靈擺卡圖在遊戲檔案內是被嚴重擠壓變形的。此模式會先將原圖進行「橫向 1.25 倍強制拉伸」還原正常視覺比例後，再從頂部進行 `1:1` 的完美正方形裁切。這能讓需要進行二次創作的畫師，直接獲得比例正確的底圖。

---

## 十一、 快捷單卡改圖 (單卡注入與大廳背景覆寫)

免去建立完整專案流程，提供單卡快速測試與系統裸檔覆寫。

<img width="1095" height="848" alt="單卡" src="https://github.com/user-attachments/assets/d357a412-561b-413a-9225-cfda5ad694aa" />

* **單卡快捷注入**：
  搜尋目標卡片 ID，選擇新圖片路徑，點擊開始替換即可直接修改。若為超框改圖，可點擊「傳送到超框改圖後處理」，系統會自動引導您調整相框透明度並完成後續動作。
* **大廳背景修改 (裸檔 Direct Replace)**：
  切換至此模式，專門用於修改遊戲大廳背景。系統會精準鎖定 `masterduel_Data/data.unity3d` 中的 `ShopBGBase02` 貼圖資產，自動進行長寬自適應縮放並完成二進位覆寫。
  [注意] `data.unity3d` 是一個極大的檔案。替換時，程式會啟動 `gc.collect()` 進行強制記憶體回收以防止系統崩潰，因此處理時間較長，請耐心等待。

---

## 十二、 超框卡片註冊器 (白名單管理與外部註冊表合併)

遊戲底層透過 `of_card_asset` 二進位註冊表來判定哪些卡片該套用 11:16 超框無邊框渲染。本模組負責該檔案的讀取、寫入與合併。

<img width="1092" height="845" alt="超框" src="https://github.com/user-attachments/assets/900b1745-7aa6-4d1e-8a13-690644eacbf2" />

### 1. 現有白名單讀取與快取基準檔 (`.pristine`)
[注意] 第一次讀取官方註冊表時，系統會在 `MD_Tool_Temp/of_card_asset_backups/` 建立一個 `.pristine` 純淨基準檔。這是工具運作的絕對基準線，**請千萬不要手動刪除它**。每次寫入新註冊表時，工具都是依據此基準檔加上您的新 ID 來進行合併。若刪除，將導致未來無法安全還原。

### 2. 外部註冊表合併與衝突分析
當您取得其他創作者提供的 `of_card_asset` 檔案時，可在上方「欲合併用註冊表」選擇該檔案並點擊合併。
[說明] 若發生衝突，程式會自動分析。若玩家註冊的 ID 在官方紀錄中指向 `0`，工具會判定為「官方保留佔位符」，並提示您保留官方設定；若指向不同目標，則跳出提示並提供刪除建議，主動阻斷合併以保護檔案結構。

### 3. 新增註冊與歷史紀錄
在下方文字框輸入欲註冊的卡片 ID，或點擊「讀取過去成功註冊的歷史紀錄」載入過往足跡。點擊「驗證衝突並輸出檔案」，工具會將名單合併寫入新的 Bundle 中。

---

## 十三、 圖形化瀏覽器 (視覺資產過濾器與沙盒系統)

提供視覺化縮圖網格，快速瀏覽場地、卡套、頭像等非卡片資產。

<img width="1053" height="837" alt="瀏覽" src="https://github.com/user-attachments/assets/2f553750-194c-4165-8ce7-420b45f93171" />

* **記憶體查表與快取生成**：
  [技術細節] 載入數百張縮圖時，如果頻繁讀取機械硬碟會導致介面卡死。工具的作法是：掃描快取資料夾建立集合，當需要渲染新圖時，使用時間複雜度 `O(1)` 的記憶體查表法取代耗時的磁碟 I/O。
* **小數點像素滑順捲動引擎**：
  [技術細節] 為了達到極致的介面體驗，工具手寫了一個線性拖曳過濾器 (`LinearDragScrollFilter`)。當玩家緩慢拖曳滑鼠時，產生的小數點步長會存入內部累加器中，直到累積滿 1 像素才會觸發畫面重繪。這徹底消除了 Qt 原生的黏滯感與慣性震盪，達到即停即止的手感。
* **跨頁面傳送**：
  選取縮圖後，點擊傳送，程式會廣域聯想並整株拔起包含主幹與所有變體（例如硬幣的正反面）。
* **無痕沙盒模式**：
  在「檔案篩選」模式下，玩家在介面上按 `Delete` 刪除圖片時，實際上只是在操作記憶體中的名單。直到點擊「確認修改並返回」，才會將結果同步回主視窗，並由系統自動銷毀沙盒。

---

## 十四、 模組串流修復器 (舊模組版本跨越移植)

當遊戲大更新導致舊版的 Mod Bundle 檔案失效、黑圖或閃退時，提供免重新製作的快速修復方案。

<img width="1101" height="841" alt="修復" src="https://github.com/user-attachments/assets/6434cb46-c528-48d8-a799-486f0e45e16c" />

[說明] 為什麼舊 Mod 會壞掉？通常是因為官方大更新覆寫了檔案，或是更改了該資產的 Hash 檔名與內部偏移量。
[技術細節] 本工具並非重新解包打包，而是執行二進位級別的「外科手術 (Inline Texture Injection)」。程式會讀取最新版的官方原檔，強制將其 `m_StreamData` 的外部路徑與偏移量歸零（斬斷官方的外部資源連結），接著將舊 Mod 的二進位像素資料 (`image_data`) 強行內嵌塞入新版檔案中，藉此完成無損升級。

---

## 十五、 虛擬模組管理器 (Symlink 零佔用掛載)

採用 Windows 系統符號連結 (Symbolic Link) 技術，實現免複製檔案、零硬碟空間佔用的模組秒掛載。

<img width="1065" height="841" alt="虛擬" src="https://github.com/user-attachments/assets/53fa520d-d56b-43b5-ad74-b7a9a06cd143" />

### 1. 掛載過濾與幽靈模組驅逐
設定掃描深度後，管理器會自動尋找內含的 0000 資料夾。
[注意] 系統會自動將名稱為「卡圖改」、「原檔」、「文件備份」等工作資料夾列入黑名單，防止玩家將未完成品誤掛載。
[技術細節] 執行掛載前，系統會進行預檢 (`filter_valid_mods`)。若發現啟用清單上的模組在硬碟上已被刪除，工具會判定其為「幽靈模組」並自動剔除，防止系統因找不到路徑而引發崩潰。

### 2. 交易回滾與 Vanilla 備份機制 (Transaction Rollback)
這是一項系統級別的安全防護。
[技術細節] 工具在掛載替換任何遊戲原檔前，會為該原檔建立 `.vanilla_backup` 備份，並將操作步驟寫入 `symlink_state.json`。如果在掛載過程中遭遇防毒軟體攔截、硬碟權限不足或檔案鎖定等異常，系統會瞬間觸發「全局回滾」。它會依照 JSON 紀錄將剛剛建立到一半的捷徑全數刪除，並將 `.vanilla_backup` 完美還原。這確保了即使遭遇斷電或錯誤，您的遊戲本體也是 100% 絕對安全的。

---

## 附錄：常見問題與排錯指南 (Troubleshooting)

* **出現 `PermissionError` 錯誤？**
  如果您在掃描、提取或改圖時遇到權限錯誤，通常是因為您正在使用 Excel 等軟體開啟了 CSV 對照表。請關閉該檔案後再次執行即可。
* **無法開啟程式或從原始碼執行時報錯？**
  如果您未下載編譯好的執行檔，而是自行從原始碼執行，若遇到音效庫缺失報錯，請確認已安裝 `msgpack` 等依賴庫。原始碼頂部建有 `MockModule` 補丁可攔截多數遺失錯誤。
* **卡片 ID 沒有產生對照名稱？**
  請檢查您在「爆搜與補齊」分頁設定的語系是否與遊戲內語言相符，或嘗試點擊「執行補齊 / 擴充」更新您的 CSV 檔案。
