[繁體中文](TUTORIAL.md) | [English](TUTORIAL_en.md)

***

# MD AstellarTool 工具操作與技術使用手冊

本手冊為《MD AstellarTool》的完整操作指引。手冊將詳細說明工具運行所需的檔案環境、圖片命名規範、介面設定，以及各個獨立功能模組的運作流程與底層邏輯。

---

## 一、 系統前置需求與圖片命名規範

在開始使用本工具進行任何修改前，請確認您的電腦環境已準備好以下目錄與檔案，並嚴格遵守圖片命名規則。

### 1. 必備的遊戲與系統路徑
* **遊戲 0000 檔案目錄**：
  遊戲本體的卡片與主要資產存放位置，路徑格式通常為：
  `[您的磁碟區]:\SteamLibrary\steamapps\common\Yu-Gi-Oh! Master Duel\LocalData\[8位數玩家ID]\0000\`
* **遊戲 StreamingAssets 目錄**（部分大檔資產）：
  `[您的磁碟區]:\SteamLibrary\steamapps\common\Yu-Gi-Oh! Master Duel\masterduel_Data\StreamingAssets\AssetBundle\`
* **遊戲大廳背景數據檔**（大廳背景修改專用）：
  `[您的磁碟區]:\SteamLibrary\steamapps\common\Yu-Gi-Oh! Master Duel\masterduel_Data\data.unity3d`

### 2. 圖片檔名命名規則 (核心匹配機制)
工具的自動化批次替換引擎是依據「檔案名稱前綴」來識別對應的卡片或資產 ID。
* **標準卡圖命名格式**：`[卡片ID]_[任意自訂文字].png` 或 `[卡片ID].png`
  * 正例：`20570_黑魔導女孩.png`、`4364.png`、`17069_異圖冰劍龍.jpg`
  * 解析邏輯：系統會以第一個底線 (`_`) 或副檔名小數點 (`.`) 為界線，向前萃取純數字。
  * 反例 (錯誤)：`黑魔導女孩_20570.png`（無法識別，前綴非數字）、`Card_20570.png`（會被當作無效檔案忽略）。
* **非卡片視覺資產命名格式**：
  若為卡套、頭像、場地等物件，檔名必須保持與對照表中的名稱一致。
  * 範例：`ProfileIcon1020001.png`、`Mat_001_BaseColor.png`

---

## 二、 設定與外觀 (系統設定首站)

本模組負責管理全域環境變數、跨分頁路徑同步、多進程算力調度、介面主題外觀，以及多語系翻譯系統。建議在初次使用時優先設定此頁面。

<img width="1095" height="847" alt="設定" src="https://github.com/user-attachments/assets/c87beb83-5276-48bf-b9c5-3f5c7a3ef661" />

 介面與全域行為設定
* **寫入 JSON 設定檔 (儲存所有變更)**：
  點擊此按鈕會將目前介面上所有的輸入框路徑、勾選項狀態、色彩設定與自訂資料夾名稱，一次性存入 `md_tool_config.json` 檔案中。底層實作了單點儲存防汙染機制，避免未確認的草稿覆寫硬碟資料。
* **跨分頁路徑同步**：
  啟用後，當您在任意頁面變更「專案根目錄」、「0000 原檔來源」或「CSV 對照表」路徑時，其他所有相關模組的對應輸入框皆會同步更新。
* **語系選擇與動態擴充**：
  設定 UI 顯示語言。下拉選單支援使用者自行手動輸入語言代碼（例如 `fr-fr`）。只要在 `MD_Tool_Essential/Languages/` 資料夾內放置對應的 JSON 翻譯檔，系統即可自動讀取。若發生部分 Key 缺失，系統具備安全降級 (Fallback) 機制，會自動顯示預設繁體中文，避免崩潰。
* **匯出待翻譯樣板**：
  點擊後，工具會啟動 Python `ast` 抽象語法樹解析器，走訪主程式原始碼，自動尋找所有被 `_("...")` 標記的字串與 UI 佔位符，並在 `Languages/` 目錄生成空白翻譯樣板 `raw.json`，同時安全更新 `zh-tw.json`。
* **最大算力 (執行緒/進程數)**：
  設定多進程並行運算的核心數。設為 `Auto` 時，程式會自動根據電腦 CPU 總核心數進行最佳化分配。

### 2. 外觀與互動體驗設定
* **五維度調色盤**：
  提供「強調色」、「背景色」、「文字色」、「側邊欄背景色」與「邊框色」五個獨立選擇器，支援 Alpha 通道透明度調配，即時套用動態 QSS 樣式表。
* **全域專案資料夾命名設定**：
  可在此統一設定預設的工作資料夾名稱（如原檔、原卡圖、文件備份、卡圖改等），系統底層在進行封裝與讀取時皆會參照此處設定。
* **側邊欄分頁排序與重命名**：
  可透過拖曳或使用上下按鈕調整左側目錄的順序，並可選取項目後修改顯示名稱。
* **自訂背景與九宮格錨點**：
  可選擇本機圖片作為 UI 背景圖。滑桿可調節背景透明度 (0~100%) 與亮度 (10~200%)。提供遊戲王 Link 箭頭九宮格選取器（西北、北、東南等），決定背景圖片在視窗縮放時的對齊錨點。

---

## 三、 新手指南與操作旅程

工具將資產修改劃分為兩種主流模式，您可以依照當下需求選擇對應的模組：
* **標準專案流水線**：適合大型模組或多檔案變更。採取「爆搜對照表 -> 找出檔案 -> 提取圖片 -> 圖檔修改 -> 寫回 Bundle -> 打包發布」的結構。
* **快捷與自動化管線**：適合單卡快速替換、大廳背景修改、一鍵全自動超框，以及模組救援與虛擬掛載。

---

## 四、 爆搜與補齊 (資料庫建立與多語系擴充)

本模組為整個工具的資料核心，負責掃描遊戲原檔二進位數據，解密文字，並建立檔案 Hash 碼與卡片內容的結構化 CSV 對照表。

<img width="1093" height="844" alt="爆搜" src="https://github.com/user-attachments/assets/9b6cfa84-eaf0-4183-8976-28b62c36321a" />

### 1. 基礎對照表生成 (全盤爆搜)
* **XOR 混淆解密與自動猜解**：
  遊戲資料庫採用 XOR 混淆加密。若遊戲更新導致預設密鑰失效，系統會啟動 `auto_crack_key` 爆破引擎，在 0 到 1023 的範圍內進行 zlib 解壓縮驗證，自動猜解出新密鑰。
* **IL2CPP Metadata 逆向映射**：
  為了解析卡套、場地等僅有 Hash 命名的配件，工具會讀取 `global-metadata.dat`，提取 ID 字串並計算 CRC32 哈希，與 `ids_item.bytes` 進行交叉比對，精準還原中文名稱。
* **記憶體防護與動態剪枝**：
  掃描過程中，系統透過 `GlobalMemoryStatusEx` API 監控實體記憶體，負載過高時自動休眠；並在 `os.walk` 走訪時剔除輸出目錄與暫存目錄，阻止無限遞迴與系統 OOM 崩潰。

### 2. 多語系橫向擴充
當您在遊戲中切換語言並下載新文字包後，可將舊有 CSV 貼入擴充欄位，修改語系代碼，點擊「執行補齊 / 擴充」。工具會比對新舊資料，在 CSV 右側自動新增新語言的名稱與效果欄位，無須重新掃描整個資料庫。

---

## 五、 找出檔案 (檔案定位與視覺化篩選)

依據 CSV 對照表，從龐大的 0000 目錄中精準尋找並複製出指定的目標 Hash 檔案。

<img width="1096" height="842" alt="找出" src="https://github.com/user-attachments/assets/3f22366e-02d1-465c-a389-159fdeea75b0" />

### 1. 操作模式選單
* **常規提取模式**：僅將目標卡片的 Hash 檔案複製到工作目錄。
* **快捷超框切換模式**：一鍵自動化模式。加入 ID 後點擊執行，系統會依序完成「複製原檔 -> 提取圖片 -> 11:16 超框裁切與 PSD 疊加 -> 寫回 Bundle -> 跳轉至超框註冊器」的全套作業。

### 2. 搜尋與視覺化沙盒篩選
* **搜尋引擎**：支援 Unicode 正規化與模糊搜尋。透過雙模式語系搜尋，即使輸入發生輕微拼寫錯誤，仍能算出相似度並列出結果。
* **視覺化篩選 (沙盒模式)**：
  點擊「視覺化篩選」，系統會將提取清單中的 ID 傳送至圖形化瀏覽器的專屬沙盒。使用者可在網格列表中檢視卡圖，選取不需要的卡片後按 `Delete` 刪除。點擊「確認修改並返回」，系統會將篩選後的名單覆寫回提取清單。

---

## 六、 提取資料 (Texture2D 導出與保護機制)

將複製出來的二進位 Bundle 檔案進行解包，導出 2D 貼圖資源。

<img width="1076" height="842" alt="提取" src="https://github.com/user-attachments/assets/91e4e574-9103-4825-9c8a-2a18666cf79e" />

* **高畫質面積比對保護機制**：
  提取圖片時，若目標路徑已存在舊檔，程式會比對新舊圖片的總像素面積（寬 x 高）。只有在新圖解析度大於或等於舊圖時才會覆蓋，保護高畫質卡圖不被低解析度縮圖覆蓋。
* **輸出控制**：支援同時匯出專屬 CSV 子對照表、圖片，以及保留原始 Bundle 備份。

---

## 七、 更改卡圖 (Bundle 二進位替換)

將外部修改好的新圖片，重新縫合寫回二進位 Bundle 檔案中。

<img width="1093" height="845" alt="改圖" src="https://github.com/user-attachments/assets/da18dfc5-efeb-470a-904e-b9bb1584c97b" />

* **執行邏輯**：
  讀取新圖片後，使用 Lanczos 高品質演算法將圖片重採樣縮放至與原始 Texture2D 完全吻合的解析度，寫入二進位數據並導出。針對卡套與頭像框等 UI 視覺資產，系統會自動套用 `MODE_FIT_VISUAL` 進行高畫質自適應縮放；對常規卡圖則嚴格保持原始解析度。

---

## 八、 封裝模組 (目錄重構與發布打包)

將修改完成的 Bundle 檔案，自動組裝為遊戲官方要求的目錄結構。

<img width="1089" height="843" alt="打包" src="https://github.com/user-attachments/assets/3fa4427a-a506-4be0-b216-30cd440d5573" />

* **目錄自動重構**：
  工具會讀取 CSV 對照表，判定每個檔案屬於常規 `0000` 還是 `StreamingAssets`。系統會在輸出資料夾中精準建立 `0000/xx/`（按 Hash 前兩位切分）資料夾，並支援一鍵壓縮為 ZIP 檔與附加 ReadMe 說明文件。

---

## 九、 智慧串連執行 (一鍵自動化流水線)

提供工廠排程功能，可將「找出檔案、提取資料、靈擺後處理、更改卡圖、封裝模組」這五個步驟串接。依賴非同步回呼佇列 (Callback Queue)，只要確保輸入路徑正確，即可一鍵無人值守完成所有流程。

<img width="1097" height="847" alt="串聯" src="https://github.com/user-attachments/assets/e7c4eb7d-f103-42f6-bb21-b08d96276016" />


---

## 十、 靈擺與超框卡圖後處理 (三大模式全解析)

專為卡片變形修復與 11:16 比例超框立體卡圖 (Over-Frame Art) 設計的影像處理中樞。

<img width="1095" height="853" alt="後處理" src="https://github.com/user-attachments/assets/59a8b6b5-fd86-4b94-ab31-a6a1a9eb0c65" />

### 1. 靈擺卡圖後處理 (卡圖改)
遊戲內的靈擺卡圖框長寬比與一般卡片不同。設定「靈擺填充比例」後，程式會在圖片下方補上對應比例的完全透明背景，增加圖片總高度，防止遊戲內渲染拉伸。底層具備雙軌判定，結合「文本關鍵字」與「二進位 6-bit 遮罩」徹底解決靈擺卡片分類模糊的問題。

### 2. 超框卡圖後處理 (卡圖改)
* **PSD 獨立圖層合成與快取**：
  超框卡圖需要 11:16 的比例。工具整合了 psd-tools，啟動 `_psd_cache` 引擎，將 PSD 拆解為五個獨立圖層（外框、名條、卡圖框、效果框、效果欄）。
* **即時預覽與參數調配**：
  展開即時預覽面板並載入測試圖片，拖動透明度滑桿 (0~100%) 時，預覽視窗會即時透過 QImage 渲染合成效果。執行時，程式會將目標圖片進行 11:16 置中裁切並疊加圖層。

### 3. 靈擺原卡圖處理 (原卡圖)
針對從遊戲中提取出來的原始靈擺卡圖進行尺寸校正。程式會將圖片進行橫向 1.25 倍拉伸，隨後進行 1:1 正方形頂部裁切，直接覆寫回原目錄，以供後續繪圖軟體作為底圖使用。

---

## 十一、 快捷單卡改圖 (單卡注入與大廳背景覆寫)

免去建立完整專案流程，提供單卡快速測試與系統裸檔覆寫。

<img width="1095" height="848" alt="單卡" src="https://github.com/user-attachments/assets/d357a412-561b-413a-9225-cfda5ad694aa" />

* **單卡快捷注入**：
  搜尋目標卡片 ID，選擇新圖片路徑，點擊開始替換即可直接修改。若為超框改圖，可點擊「傳送到超框改圖後處理」，系統會自動引導您調整相框透明度。
* **大廳背景修改 (裸檔 Direct Replace)**：
  切換至此模式，專門用於修改遊戲大廳背景。系統會精準鎖定 `masterduel_Data/data.unity3d` 中的 `ShopBGBase02` 貼圖資產，自動進行長寬自適應縮放並完成二進位覆寫。

---

## 十二、 超框卡片註冊器 (白名單管理與外部註冊表合併)

遊戲底層透過 `of_card_asset` 二進位註冊表來判定哪些卡片該套用 11:16 超框無邊框渲染。本模組負責該檔案的讀取、寫入與合併。

<img width="1092" height="845" alt="超框" src="https://github.com/user-attachments/assets/900b1745-7aa6-4d1e-8a13-690644eacbf2" />

### 1. 現有白名單讀取與快取基準檔 (`.pristine`)
自動搜尋 0000 目錄中的 `of_card_asset` 檔案，將二進位 `<HH` (uint16) 結構解包為文字。第一次讀取時，系統會在暫存區建立不可變的 `.pristine` 純淨基準檔，寫入時以此為基準線，防止多次讀寫造成結構損壞。

### 2. 外部註冊表合併 (Merge Feature)
當您取得其他創作者提供的 `of_card_asset` 檔案時，可在上方「欲合併用註冊表」選擇該檔案並點擊合併。系統會解析外部 ID 陣列進行聯集合併。
* **衝突分析**：若發生衝突，程式會自動分析。若玩家註冊的 ID 在官方紀錄中指向 0，判定為「官方保留佔位符衝突」；若指向不同目標，則跳出提示並提供刪除建議，阻斷合併以保護檔案結構。

### 3. 新增註冊與歷史紀錄
在下方文字框輸入欲註冊的卡片 ID。可點擊「讀取過去成功註冊的歷史紀錄」載入過往足跡。點擊「驗證衝突並輸出檔案」，工具會將上下兩部分的白名單合併，寫入新的 Bundle。

---

## 十三、 圖形化瀏覽器 (視覺資產過濾器與沙盒系統)

提供視覺化縮圖網格，快速瀏覽場地、卡套、頭像等非卡片資產。

<img width="1053" height="837" alt="瀏覽" src="https://github.com/user-attachments/assets/2f553750-194c-4165-8ce7-420b45f93171" />

* **記憶體查表與網頁級線性拖曳**：
  載入縮圖時，工具將硬碟 I/O 轉為記憶體查表，達到毫秒級載入。介面實作了自訂的線性拖曳過濾器 (`LinearDragScrollFilter`)，透過小數點像素累積器抹除 Qt 預設拖曳的物理慣性，提供如同現代網頁瀏覽器般的即停即止順暢體驗。
* **跨頁面傳送**：
  選取縮圖後，點擊傳送至提取清單，程式會廣域聯想並整株拔起包含主幹與所有變體（例如硬幣的正反面）。

---

## 十四、 模組串流修復器 (舊模組版本跨越移植)

當遊戲大更新導致舊版的 Mod Bundle 檔案失效、黑圖或閃退時，提供免重新製作的修復方案。

<img width="1101" height="841" alt="修復" src="https://github.com/user-attachments/assets/6434cb46-c528-48d8-a799-486f0e45e16c" />

* **內嵌注入技術 (Inline Texture Injection)**：
  遊戲更新有時會變更卡片檔案Bundle。本工具會讀取最新版的官方原檔，將其 `m_StreamData` 的路徑與偏移量強制歸零（截斷外部串流），隨後直接將舊 Mod 的貼圖 Raw Bytes 內嵌寫入新檔的 `image_data` 中，修復模組。

---

## 十五、 虛擬模組管理器 (Symlink 零佔用掛載)

採用 Windows 系統符號連結 (Symbolic Link) 技術，實現免複製檔案、零硬碟空間佔用的模組秒掛載。

<img width="1065" height="841" alt="虛擬" src="https://github.com/user-attachments/assets/53fa520d-d56b-43b5-ad74-b7a9a06cd143" />

* **層層穿透與黑名單過濾**：
  支援設定 1~5 層的掃描深度，自動尋找內含的 0000 資料夾。掛載時自動過濾使用者設定的工作資料夾（如文件備份、卡圖改），防止備份資料錯誤掛載。
* **優先權排序與覆寫日誌**：
  左側清單為「已啟用模組」，位置越偏上方擁有越高優先權。若發生同名檔案衝突，程式會依據順序覆寫，並在日誌區域明確標註衝突覆蓋資訊。
* **安全回滾與 Vanilla 備份機制 (Transaction Rollback)**：
  套用前，系統會對遊戲原始檔案建立 `.vanilla_backup` 備份。建立符號連結過程中，若遇到任何檔案鎖定或權限異常錯誤，系統會立即暫停作業，並依據 `symlink_state.json` 紀錄自動進行全局回滾，將所有已建立的連結刪除並還原原廠檔案，確保遊戲本體絕對安全。

---

## 十六、 底層引擎與進階技術補充

除了上述操作介面，本工具在底層實作了以下確保穩定性與擴充性的進階技術：

* **EXE 打包防閃退補釘 (`MockModule`)**：
  透過 `types.ModuleType` 建立虛擬模組並替換 `sys.modules` 中的音效庫，解決 PyInstaller 打包為獨立 `.exe` 時因缺少原生 DLL 而導致啟動崩潰的難題。
* **滾輪事件強制吸收 (`prevent_scroll_propagation`)**：
  攔截所有下拉選單與清單的滾輪事件，避免清單滾到底部時帶動外層背景滾動，或是在調整設定時發生誤觸。
* **防汙染草稿單點儲存 (`save_single_key`)**：
  設定檔採用單一數值直接覆寫磁碟 JSON 檔案的技術，不將未確認的 UI 草稿同步寫入，保障設定檔的純淨與穩定。
