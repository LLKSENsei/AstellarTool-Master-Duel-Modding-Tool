[繁體中文](TUTORIAL.md) | [English](TUTORIAL_en.md)

***

# MD AstellarTool 工程操作與技術深度手冊

本手冊為《MD AstellarTool》的完整工程規範與技術使用指引。本文檔不使用任何誇飾用語或表情符號，旨在精準、深入且嚴謹地揭示本工具的底層運作機制、影像算法、記憶體管理策略、二進位數據流與安全防護架構。

使用者將能透過本文檔了解每個輸入參數的實體意義、所有按鈕與開關背後觸發的系統層級行為、獨立分頁的操作步驟，以及跨模組的實戰組合案例。

---

## 第一章：系統路徑與資源命名規格

工具的批次自動化引擎高度依賴精確的路徑掛載與檔名正則表示式 (Regular Expression) 萃取。在開始使用前，必須確保符合以下規範。

### 1.1 核心實體路徑定義
* **遊戲 0000 檔案目錄 (0000 Directory)**
  存放遊戲本體卡片與主要資產二進位 AssetBundle 的根目錄。
  預設路徑格式：`[磁碟區]:\SteamLibrary\steamapps\common\Yu-Gi-Oh! Master Duel\LocalData\[8位數玩家ID]\0000\`
* **遊戲 StreamingAssets 目錄 (StreamingAssets Directory)**
  存放高解析度素材或大型資料包的目錄。
  預設路徑格式：`[磁碟區]:\SteamLibrary\steamapps\common\Yu-Gi-Oh! Master Duel\masterduel_Data\StreamingAssets\AssetBundle\`
* **遊戲大廳數據包檔案 (Lobby Background Data)**
  單一二進位封包檔案，存放包含大廳背景貼圖 `ShopBGBase02` 在內的介面資源。
  預設路徑格式：`[磁碟區]:\SteamLibrary\steamapps\common\Yu-Gi-Oh! Master Duel\masterduel_Data\data.unity3d`

### 1.2 資源命名與正規化解析機制
工具在進行二進位封裝或資料庫比對時，會透過 Python 的 `re` 模組剝離使用者自訂綴詞。

* **卡片圖檔命名規範**
  * **規則**：`[卡片ID]_[任意自訂標籤].[副檔名]` 或 `[卡片ID].[副檔名]`
  * **底層機制**：工具採用 `^([a-zA-Z]*)(\d+)(.*)$` 進行萃取。系統會忽略首部的英文字母，強制鎖定字串中「連續的數字區塊」作為卡片 ID，並將後續的所有字元（包含底線、空白等）視為無效後綴加以剝離。
  * **正確範例**：`20570.png`、`20570_赫之聖女.png`、`Alt17069_自訂圖.jpg`。
  * **錯誤範例**：`赫之聖女_20570.png`（解析器無法於字串起始處捕捉數字，將導致匹配失敗）。
* **非卡片視覺資產 (UI / 場地 / 卡套)**
  * **規則**：必須與從遊戲提取出的對照表名稱一字不差（大小寫不敏感）。
  * **底層機制**：工具使用 `unicodedata.normalize('NFKC', text).lower()` 進行雙向同化比對，消除全半形與大小寫差異。
  * **範例**：`Mat_001_BaseColor.png`、`ProfileIcon1020001.png`。

---

## 第二章：全介面分頁功能解析與單頁操作 SOP

本章節按照軟體介面預設順序，詳細解剖所有分頁的介面元件、輸入框、開關、按鈕與操作 SOP。

---

### 1. 設定與外觀 (Tab 7, `t7_settings`) 【首要配置頁面】

<img width="1095" height="847" alt="設定" src="https://github.com/user-attachments/assets/c87beb83-5276-48bf-b9c5-3f5c7a3ef661" />

#### 技術機制與運作原理
本模組為工具的全域設定中樞，負責 JSON 設定檔的寫入與讀取、跨分頁路徑同步信號 (Signal) 的發送，以及動態 QSS 樣式表渲染。
* **原子化寫入防汙染機制 (`_atomic_write_json`)**：當點擊寫入設定時，系統會先使用 `RLock` 鎖定執行緒，剔除巨大記憶體快取（如 `t2_extraction_list`），將乾淨的字典寫入 `.tmp` 暫存檔，最後使用 `os.replace` 進行物理原子置換。此機制能確保即使寫入期間發生斷電，設定檔也不會變成 0KB 損壞檔。
* **AST 抽象語法樹原始碼解析器 (`task_generate_translation_template`)**：啟動後會調用 Python `ast` 模組，走訪主程式原始碼，抓取所有以 `_("...")` 包裹的字串與 UI 佔位符 (`placeholder`)，自動生成 `MD_Tool_Essential/Languages/raw.json` 語系樣板，並無損補齊 `zh-tw.json` 的缺漏項目。

#### 全介面元件與輸入框詳解
* **跨分頁路徑同步 (Checkbox)**：啟用後，於任何頁面修改「專案根目錄」、「0000 來源」或「CSV 字典」路徑時，會透過 ConfigSignals 廣播更新全頁面對應的輸入框。
* **完成後自動切換分頁 (Checkbox)**：啟用後，單項任務完成時，系統會自動跳轉至下一個對應的處理頁面。
* **語系 (重啟生效) (ComboBox)**：設定介面語言（預設 `zh-tw`、`en-us`）。可自由手動輸入其他語言代碼（如 `ja-jp`），系統會自動嘗試讀取 `Languages/[代碼].json`。
* **最大算力(執行緒) (ComboBox)**：設定多進程池 (`ProcessPoolExecutor`) 的 CPU 核心數。可輸入數字或設為 `Auto`。設為 `Auto` 時，系統會自動留出 1 至 4 個核心供作業系統運作，防止 CPU 使用率 100% 造成介面凍結。
* **指定字體 (ComboBox)**：選擇介面顯示字體。
* **自訂五維度調色盤 (Buttons)**：提供「強調色」、「背景色」、「文字色」、「側邊欄背景色」、「邊框色」5 個色塊。點擊會彈出 `QColorDialog`，支援 Alpha 通道選色，並即時重構 QSS 樣式表套用至整個視窗。
* **全域專案資料夾命名設定 (LineEdits)**：設定「原檔」、「原卡圖」、「文件備份」、「卡圖改」、「改完的文件」、「對照表 CSV」等資料夾的預設名稱。
* **側邊欄分頁排序與重命名 (ListWidget & LineEdit)**：列出所有分頁。可點選項目後使用 [▲]、[▼] 按鈕調整側邊欄順序，或在輸入框輸入新名稱後點擊 [重命名]。
* **背景圖片 (Path Row)**：設定本機圖片作為 GUI 背景圖。
* **透明度 (Slider)**：調整介面半透明度 (0 至 100%)。
* **亮度 (Slider)**：調整背景圖片亮度 (10 至 200%)。
* **九宮格錨點 (LinkAnchorPicker)**：提供九宮格位置選取，決定背景圖於視窗縮放時的固定錨點。

#### 按鈕功能詳解
* **[寫入 JSON 設定檔 (儲存所有變更)]** (頂部與底部各一)：將目前的設定值保存至 `md_tool_config.json`。
* **[匯出待翻譯樣板 (raw.json)]**：執行 AST 語法樹解析，產出語系樣板檔。

#### 本分頁獨立操作 SOP 與路徑保存規範
1. 於「全域專案資料夾命名設定」中，確認各資料夾名稱符合您的工作習慣。
2. **重要路徑保存規範**：點擊 [寫入 JSON 設定檔] 時，系統會記錄介面上所有的輸入框。為避免將一次性的臨時專案路徑寫入全域設定，在點擊儲存前，請先清空其他分頁中臨時輸入的專案子目錄，僅保留「0000 遊戲目錄」、「主 CSV 對照表路徑」以及「預設資料夾名稱」。
3. 點擊 **[寫入 JSON 設定檔 (儲存所有變更)]**。下次開啟工具時，臨時欄位會保持空白，方便直接貼上新專案路徑。

---

### 2. 新手指南與操作旅程 (Tab 0, `t0_guide`)

#### 技術機制與運作原理
本模組為靜態資訊展示頁面，使用唯讀的 `QPlainTextEdit` 渲染系統指南與聲明，不消耗系統運算資源。

#### 介面元件與操作
* 內建全功能說明文本，使用者可在此瀏覽標準專案流程、快捷管線與各分頁的作用介紹。

---

### 3. 爆搜與補齊 (Tab 1, `t1_scan`)

<img width="1093" height="844" alt="爆搜" src="https://github.com/user-attachments/assets/9b6cfa84-eaf0-4183-8976-28b62c36321a" />

#### 技術機制與運作原理
本模組負責遍歷 0000 目錄下的二進位檔案，構建卡片 ID、真實名稱、效果文字與 Hash 編碼的關聯性 CSV 字典。
* **XOR 動態猜解引擎**：官方將卡片文字庫加密。系統預設使用 `0x3D (61)` 進行 XOR 位元反轉。若遇官方更新導致解密失敗，引擎將觸發 `auto_crack_key` 函式，在 0 至 1023 範圍內暴力輪詢，解壓驗證 zlib 封包結構，成功後自動捕捉新密鑰並寫回設定檔。
* **6-bit 物理遮罩判定**：針對屬性判讀，引擎直接讀取 `card_prop.bytes` 二進位位移。若位元值為 `0x020D` 判定為魔法卡；怪獸卡則提取前 6 個位元 (6-bit mask) 進行實體特徵碼比對（如 `0x19`, `0x24` 對應靈擺），並輔以 NLP 效果文關鍵字掃描，實現精準的卡片分類。
* **IL2CPP Metadata 碰撞**：針對無卡片 ID 的配件資產（如頭像、場地），讀取 `global-metadata.dat` 提取 ID 字串並計算 CRC32 哈希，與 `ids_item.bytes` 進行雙向碰撞比對，還原名稱。
* **記憶體防呆監控**：多進程掃描時，呼叫 Windows `GlobalMemoryStatusEx` API。當實體記憶體負載達 85% 閾值時，強制休眠並等待 GC 回收，防止系統死機。

#### 全介面元件與 10 大開關細節剖析
* **目標資料夾 (0000)**：指定遊戲 0000 資料夾路徑。
* **儲存根目錄**：專案輸出位置，後續所有操作皆以此為基準。
* **TXT / CSV 命名 (LineEdits)**：設定輸出的清單檔與字典檔檔名。
* **1. 啟用大小過濾 (Checkbox, `t1_size_filter`)**：
  * **開啟 (ON)**：限制僅掃描檔案大小於「下限(KB)」與「上限(KB)」範圍內之 Bundle 檔，避開無關微型檔與巨型檔。
  * **關閉 (OFF)**：無差別掃描目標目錄內的所有 Bundle 檔。
* **2. 只搜卡片 (Checkbox, `t1_only_numbers`)**：
  * **開啟 (ON)**：僅萃取純數字 ID 之卡片資產，忽略帶有英文字母的場地、卡套等配件。
  * **關閉 (OFF)**：同時萃取純數字卡片與帶有英文字母的配件資產。
* **3. 產生內容物列表(TXT) (Checkbox, `t1_gen_txt`)**：
  * **開啟 (ON)**：自動生成包含所有 ID 的 TXT 清單。
  * **關閉 (OFF)**：不生成 TXT 清單。
* **4. 產生對照表(CSV) (Checkbox, `t1_gen_csv`)**：
  * **開啟 (ON)**：自動導出包含目錄、Container、Hash 與 ID 的 CSV 主字典。
  * **關閉 (OFF)**：不導出 CSV 對照表（逆向導出已修改目錄的 TXT 時使用）。
* **5. 附帶卡片名稱 (Checkbox, `t1_ext_name`)**：
  * **開啟 (ON)**：於 CSV 寫入指定語系之卡片名稱。
  * **關閉 (OFF)**：CSV 不寫入卡片名稱。
* **6. 附帶卡片效果 (Checkbox, `t1_ext_desc`)**：
  * **開啟 (ON)**：於 CSV 寫入卡片效果內文，方便日後以效果關鍵字搜卡。
  * **關閉 (OFF)**：CSV 不寫入效果內文，可大幅縮小 CSV 體積並加快讀取速度。
* **7. 附帶配件名稱 (Checkbox, `t1_parse_meta`)**：
  * **開啟 (ON)**：讀取 `global-metadata.dat` 還原卡套、頭像等配件之名稱。
  * **關閉 (OFF)**：不解析 Metadata，配件顯示原始代碼。
* **8. 優先使用暫存 (Checkbox, `t1_use_cache`)**：
  * **開啟 (ON)**：讀取 `MD_Tool_Temp/` 上次解密留下的暫存檔，實現秒級掃描。
  * **關閉 (OFF)**：強制重新解密遊戲 Bundle。遊戲大更新後必須關閉此項以獲取新數據。
* **9. 深度掃描 (Checkbox, `t1_deep_scan`)**：
  * **開啟 (ON)**：深層解包 Bundle 內部所有物件，提取藏在巨型資產包內的場地貼圖或大廳背景。
  * **關閉 (OFF)**：僅匹配頂層 Container 路徑，掃描速度極快。
* **10. 僅處理場地/卡套/頭像等物件 (Checkbox, `enable_visual_only_filter`)**：
  * **開啟 (ON)**：過濾掉純數字常規卡片，掃描結果僅保留配件資產。
  * **關閉 (OFF)**：按常規規則掃描所有資產。
* **語系 (ComboBox) / XOR 密鑰 (LineEdit)**：解密語言與 key。
* **待擴充的 CSV (Path Row)**：供補齊功能使用之舊 CSV 路徑。

#### 按鈕功能詳解
* **[開始掃描]**：啟動多進程掃描，完成後生成 TXT 清單與 CSV 字典。
* **[執行補齊 / 擴充]**：讀取「待擴充的 CSV」，將新語系欄位 Append 至 CSV 右側。
* **[停止爆搜 / 中斷作業]**：發送 `multiprocessing.Event()` 中斷信號，終止子進程。

#### 本分頁獨立操作 SOP
* **SOP 1.1：遊戲 0000 完整目錄掃描**
  1. 填寫「目標資料夾 (0000)」與「儲存根目錄」。
  2. 勾選「產生對照表(CSV)」、「附帶卡片名稱」、「附帶配件名稱」。
  3. 點擊 **[開始掃描]**。完成後於根目錄產出 `2DTexture_Mapping.csv`。
* **SOP 1.2：舊 CSV 多語系對照表擴充**
  1. 於「待擴充的 CSV」選擇先前做好的 CSV 檔案。
  2. 將「語系」切換為目標新語言（例如 `ja-jp`）。
  3. 點擊 **[執行補齊 / 擴充]**。系統會自動補齊新語言欄位並儲存。

---

### 4. 找出檔案 (Tab 2, `t2_find`)

<img width="1096" height="842" alt="找出" src="https://github.com/user-attachments/assets/3f22366e-02d1-465c-a389-159fdeea75b0" />

#### 技術機制與運作原理
依據 CSV 字典，從 0000 目錄中精準尋找並複製出目標二進位 Hash 檔案。
* **Google-like 進階分詞檢索引擎 (`search_cards_advanced`)**：系統內建高效能記憶體檢索矩陣，支援布林邏輯與精準詞綴過濾。在輸入框中可使用以下語法：
  * **空白分隔**：視為 `AND` 邏輯，需同時滿足所有關鍵字。
  * **引號鎖定 (`"關鍵字"`)**：強制精準匹配引號內的完整詞組，不進行分詞。
  * **強制包含 (`+關鍵字`)**：目標必須包含此關鍵字（卡名或效果皆可）。
  * **強制排除 (`-關鍵字`)**：目標若包含此關鍵字則直接剔除（例如排除特定系列）。
* **矩陣式屬性判定 (Matrix Filter)**：透過解析 `card_prop.bytes` 的 6-bit 物理遮罩，提供三大類（怪獸、魔法、陷阱）共 18 種子屬性的精準交叉比對。
* **無慣性線性捲動 (`LinearDragScrollFilter`)**：注入底層 EventFilter，將滑鼠拖曳轉化為小數點步長累加，實現網頁級別的即停即止捲動，徹底消除 Qt 預設的震盪與回彈。
* **動態記憶體分頁切片**：搜尋結果支援單頁 50 至無限大筆數的 0ms 瞬間渲染切片，避免萬筆資料同時寫入 UI 導致執行緒凍結。

#### 全介面元件與輸入框詳解
* **選擇模式 (ComboBox)**：
  * `MODE_NORMAL` (常規提取)：純粹將 Hash 檔複製至「原檔」資料夾。
  * `MODE_QUICK` (快捷超框切換)：啟動全自動化超框寫入管線。重要限制：快捷超框切換模式僅適用於原廠遊戲卡圖。自訂卡圖請改用 Tab 8。
* **對照語系與搜尋框 (ComboBox & LineEdit)**：設定搜尋目標語言。搜尋框支援前述的進階檢索語法。
* **收納式篩選面板 (Filter Panel)**：
  * **怪獸區**：提供效果、通常、融合、同步、超量、連結、儀式、靈擺、衍生物等實體卡片種類過濾。
  * **魔法區**：提供通常、速攻、永續、場地、裝備、儀式過濾。
  * **陷阱區**：提供通常、反制、永續過濾。
  * **輔助與進階區**：包含「僅處理場地/卡套/頭像等物件」開關、卡名與效果文的模糊比對容錯開關（演算法基於 SequenceMatcher 滑動視窗），以及單頁顯示上限設定。
* **對照表 (CSV) / 載入用 TXT / 檔案來源 (0000) / 儲存根目錄 (Path Rows)**：設定各階段讀寫路徑。若路徑未即時刷新，於輸入框中間點擊空白鍵再刪除，即可強制觸發更新。
* **新建資料夾名稱 (LineEdit)**：存放複製出之二進位檔案的資料夾名稱（預設 `原檔`）。
* **分頁導覽列 (Navigation Bar)**：提供上一頁、下一頁切換，以及「加入本頁全部」與「加入全部搜尋結果」的批次寫入按鈕。
* **搜尋結果列表 (ListWidget) / 提取清單 (PlainTextEdit)**：展示搜尋項目與最終欲提取之 ID 清單。

#### 按鈕功能詳解
* **[開始提取原檔] / [開始自動化超框更改]** (頂部與底部)：執行檔案複製或啟動自動化超框管線。
* **[載入 TXT]**：從外部文字檔載入 ID 至提取清單。
* **[正規化補名]**：反查 CSV 字典，將文字框中的 ID 重新排版為 `ID (名稱) [Hash]` 格式。
* **[視覺化篩選]**：啟動沙盒模式，將提取清單中的 ID 發送至 Tab 12 生成圖片快取供人工視覺剔除。
* **[確認 ID 並返回超框註冊器]**：(連動顯示) 攜帶清單內之 ID 跳轉返回 Tab 13。
* **清單雙擊與右鍵操作**：雙擊搜尋結果列表項目，或反白後按右鍵，可快速將項目加入提取清單。

#### 本分頁獨立操作 SOP
* **SOP 2.1：常規原檔提取**
  1. 確保模式選單為「常規提取」。
  2. 展開「篩選」面板設定卡片種類，或於搜尋框輸入進階語法（例如：`"阿不思" -融合`）。
  3. 於結果列表雙擊項目，或點擊分頁列的「加入本頁全部」，將 ID 送入提取清單。
  4. 點擊 **[開始提取原檔]**。目標二進位檔案將複製至 `專案目錄/原檔/`。
* **SOP 2.2：原廠卡圖快捷超框全自動處理**
  1. 模式選單切換至「快捷超框切換」。
  2. 於提取清單輸入原廠卡片 ID。
  3. 點擊 **[開始自動化超框更改]**。
  4. 內部管線將依序自動完成：提取原檔 -> 解包原圖並備份 Bundle -> 複製圖片 -> 執行 11:16 裁切與 PSD 相框合成 -> 寫回 Bundle -> 帶入卡片 ID 送入 Tab 13 註冊器。
---

### 5. 提取資料 (Tab 3, `t3_extract`)

<img width="1076" height="842" alt="提取" src="https://github.com/user-attachments/assets/91e4e574-9103-4825-9c8a-2a18666cf79e" />

#### 技術機制與運作原理
使用 UnityPy 對 Bundle 進行解包，導出 `Texture2D` (圖檔) 與 `TextAsset` (文字描述)。
* **面積保護機制**：寫入圖片前比對像素總面積 `(w * h) <= (ext_img.w * ext_img.h)`。若解包出的解析度小於現有硬碟檔，則放棄寫入，保護已修改之高畫質圖檔。
* **貼圖過濾 (`is_valuable_texture`)**：自動封鎖 3D 渲染貼圖，僅導出有用之 2D 視覺資產。

#### 全介面元件與輸入框詳解
* **欲提取檔案所在資料夾 (Path Row)**：指定輸入路徑（通常指向 `原檔`）。
* **匯出 CSV / 匯出圖片 / 匯出文字資料 / 備份檔案 (Checkboxes)**：控制解包導出項目。
* **僅處理場地/卡套/頭像等物件 (Checkbox)**：過濾非卡片資產。
* **圖片資料夾名稱 (LineEdit)**：導出圖片之存放目錄（預設 `原卡圖`）。

#### 按鈕功能詳解
* **[開始提取內容物]**：啟動多進程解包。圖片寫入 `原卡圖/`，原始 Bundle 複製至 `文件備份/`。

#### 本分頁獨立操作 SOP：解包導出
1. 確認「欲提取檔案所在資料夾」指向 `專案目錄/原檔`。
2. 勾選「匯出圖片」與「備份檔案」。
3. 點擊 **[開始提取內容物]**。導出之 PNG 將儲存於 `專案目錄/原卡圖/`。

---

### 6. 更改卡圖 (Tab 4, `t4_replace`)

<img width="1093" height="845" alt="改圖" src="https://github.com/user-attachments/assets/da18dfc5-efeb-470a-904e-b9bb1584c97b" />

#### 技術機制與運作原理
將外部修改後之圖片重新縫合寫回二進位 Bundle。
* **Lanczos 重採樣 (`MODE_FIT_VISUAL`)**：若屬非卡片 UI 資產，系統強制套用 Lanczos 演算法縮放至與原廠規格完全吻合，防止 UI 破圖。
* **色彩空間校正**：針對 Spine 動畫貼圖 (`p[數字]`)，寫入時強制將 `m_ColorSpace` 設為 `0` (Gamma 空間)，消除螢光色偏。
* **原子寫入**：寫入 `.tmp` 後以 `os.replace` 物理置換，防止斷電產生 0KB 損壞檔。

#### 全介面元件與輸入框詳解
* **字典 (CSV) 檔案 / 專案根目錄位置 (Path Rows)**：全域讀寫路徑。
* **備份位置 (LineEdit)**：存放未修改 Bundle 的目錄（預設 `文件備份`）。
* **改圖位置 (LineEdit)**：存放已修改 PNG/JPG 的目錄（預設 `卡圖改`）。

#### 按鈕功能詳解
* **[開始替換]**：讀取「卡圖改」之圖片，寫回「文件備份」對應之 Bundle，產物儲存至 `改完的文件/`。

#### 本分頁獨立操作 SOP：圖片寫回 Bundle
1. 將修改好的圖檔命名為 `[卡片ID].png` 並放入 `專案目錄/卡圖改/`。
2. 確認備份位置指向 `文件備份`。
3. 點擊 **[開始替換]**。修改後的 Bundle 將生成於 `專案目錄/改完的文件/`。

---

### 7. 封裝模組 (Tab 5, `t5_package`)

<img width="1089" height="843" alt="打包" src="https://github.com/user-attachments/assets/3fa4427a-a506-4be0-b216-30cd440d5573" />

#### 技術機制與運作原理
將修改完成之 Bundle 依據 Master Duel 原廠檔案系統規格進行目錄層級重構。
* **哈希目錄切分**：依據 CSV 對照表，若屬 `0000` 資源，自動擷取 Hash 碼前兩位（如 `a589...` -> `a5`），建立 `0000/a5/` 子目錄並移入檔案。

#### 全介面元件與輸入框詳解
* **字典 (CSV) 檔案 / 專案根目錄 (Path Rows)**：讀寫路徑。
* **資料夾命名 (LineEdit)**：輸出模組目錄名稱（預設 `ModFolder`）。
* **附帶「卡圖改」 / 附帶 ReadMe.txt / 同時壓縮成 ZIP (Checkboxes)**：打包控制項。
* **ReadMe 預設文字 (PlainTextEdit)**：自訂說明文件內文。

#### 按鈕功能詳解
* **[開始打包]**：建立結構、複製檔案並執行 ZIP 壓縮。

#### 本分頁獨立操作 SOP：打包發布
1. 設定「資料夾命名」，並勾選「同時壓縮成 ZIP」。
2. 點擊 **[開始打包]**。根目錄將產出可供發布之模組壓縮檔。

---

### 8. 智慧串連執行 (Tab 6, `t6_chain`)

<img width="1097" height="847" alt="串聯" src="https://github.com/user-attachments/assets/e7c4eb7d-f103-42f6-bb21-b08d96276016" />

#### 技術機制與運作原理
實作非同步 Callback Queue 佇列驅動，無人值守自動化執行多步驟。

#### 全介面元件與按鈕
* 勾選項：[2] 找出檔案、[3] 提取資料、[8] 靈擺後處理、[4] 更改卡圖、[5] 封裝模組。
* **[▶ 開始自動化作業]**：依序執行已勾選之任務。

#### 本分頁獨立操作 SOP：一鍵自動化
1. 確保相關分頁之路徑與設定皆已填寫完成。
2. 勾選欲執行的步驟（如 [2]、[3]、[4]、[5]）。
3. 點擊 **[▶ 開始自動化作業]**。

---

### 9. 靈擺與超框卡圖後處理 (Tab 8, `t8_pendulum`)

<img width="1095" height="853" alt="後處理" src="https://github.com/user-attachments/assets/59a8b6b5-fd86-4b94-ab31-a6a1a9eb0c65" />

#### 技術機制與運作原理
本模組為工具的核心影像處理中樞，提供 4 大影像處理模式與極高自由度的圖層渲染管線：
1. **靈擺卡圖後處理 (`MODE_PENDULUM`)**：於圖片底部填充透明背景（依 `pad_pct` 比例），修正靈擺卡圖在遊戲內的正方形拉伸變形。
2. **超框卡圖後處理 (`MODE_OVERFRAME`)**：
   * **單層精修與雙圖層合成 (-ch / -bg 邏輯)**：若輸入圖片命名為 `[ID]-ch.png`（角色層）與 `[ID]-bg.png`（背景層），系統將自動啟動進階雙圖層工坊，允許獨立調整角色與背景的 X/Y 軸平移、縮放與旋轉。若為普通單圖，則自動執行 11:16 滿版裁切。
   * **動態 Z-Order 幾何合成**：讀取 `CardFrame/` PSD 檔，並允許使用者自訂 8 大圖層（角色、背景、外框、卡名欄、卡圖框、效果框、效果文字欄、卡底背景）的上下堆疊順序。
   * **閃卡光澤與遮罩生成矩陣 (Foil Studio)**：內建餘弦光譜調變 (IQ Cosine Palette) 與雙軌混色系統 (Pegtop Soft Blend / Vivid Overdrive)，提供珍珠粉彩、霓虹光譜、香檳金、白金鑽四種光學模擬。可獨立控制各部件的預覽發光、實體烙印 (Bake) 以及透明挖空 (Dirty Alpha)。
3. **卡片動畫後處理 (`MODE_CUTIN`)**：讀取 PSD、MP4、GIF 或單張靜態圖片。
   * **色彩修飾與去背 (Chroma Key & Despill)**：內建綠幕去背演算法，並提供 Despill 溢色修正，針對人物邊緣的綠色反光進行色相偏移抑制。
   * **Spine 骨骼生成與 PMA 預乘 Alpha**：透過 4 點 `popup_curve` 時間軸生成 Spine 4.2.43 相容的 JSON 動畫數據。導出前強制執行 PMA (Premultiplied Alpha) 像素預乘，徹底消滅 Unity 渲染時的邊緣黑線雜訊，並自動輸出符合 16:9 比例的 HD 與 SD 雙規格圖集。
4. **靈擺原卡圖處理 (`MODE_PENDULUM_ORIGINAL_CROP`)**：將提取出之原廠扭曲圖片進行 1.25 倍橫向拉伸，並從頂點進行 1:1 正方形裁切還原。

#### 全介面元件與輸入框詳解
* **處理模式 (ComboBox)**：切換上述 4 種運作管線。
* **專案與路徑設定區**：設定 CSV 字典、專案根目錄、改圖位置與原卡圖位置。包含靈擺填充比例與備份資料夾命名控制。
* **超框要素透明度控制 (Sliders)**：精細調整 PSD 6 大組件之 Alpha 透明度 (0~100%)。
* **閃卡光澤與遮罩設定 (Foil Studio)**：
  * **3x6 控制矩陣**：控制外框、卡名欄等 6 個部件是否參與「預覽模擬 (Preview)」、「寫入貼圖 (Bake)」、「透明化遮罩 (Alpha 0)」。
  * **光學參數滑桿**：光譜風格、混色模式、珠光基底明度、高光聚光度 (Sharpness)、光澤強度、色彩濃度、光斑頻率與光照角度。
* **即時預覽面板**：提供單幀記憶體光速抽幀預覽。勾選「模擬遊戲內閃卡光澤」可即時觀看 Foil 演算法疊加效果。
* **進階雙圖層超框工坊 (-ch / -bg 專用)**：
  * 提供角色圖層與背景圖層獨立的 X/Y 平移、縮放、旋轉滑桿。
  * 可透過顏色選擇器自訂無背景時的純色底板。
  * **圖層自由排序面板 (Z-Order)**：可透過拖曳或按鈕，自由改變 8 大圖層的上下覆蓋關係。
* **素材與畫布設定區 (Cut-In 動畫專用)**：設定 HD 輸出解析度 (1920x1080 / 1280x720)、畫布填充模式 (裁切滿版 / 等比留邊 / 拉伸)、旋轉角度與 X/Y 軸整體平移。勾選「啟用硬碟暫存」可防止處理長影片時記憶體溢出。
* **關鍵幀預覽時間控制區 (Cut-In 動畫專用)**：預覽時間點微調滑桿，提供跳至初始點、峰值、沉澱、結束點的快捷按鈕。
* **綠幕去背與色彩修飾區 (Cut-In 動畫專用)**：包含色彩去背開關、防溢色修飾開關、去背色盤。提供去背容錯度、邊緣羽化、溢色修正強度、亮度、對比度、暗角邊框強度滑桿。
* **時間控制與 POP UP 特效區 (Cut-In 動畫專用)**：設定剪輯起點、動畫總長度、輸出影格率 (FPS)、播放倍速。包含 4 點 Spine 縮放曲線設定 (時間與尺寸)，並提供「生成全尺寸真實預覽 GIF」功能。
* **待處理清單 (PlainTextEdit)**：手動輸入或透過「自動掃描目標目錄」載入符合條件之 ID。點擊清單內的 ID 會自動帶入預覽路徑。

#### 本分頁獨立操作 SOP
* **SOP 8.1：靈擺圖片填充與原圖修復**
  1. 模式選擇「靈擺卡圖後處理」或「靈擺原卡圖處理」。
  2. 點擊 **[自動掃描目標目錄]** 載入 ID。
  3. 點擊 **[🚀 開始執行後處理]** 覆寫產物。
* **SOP 8.2：超框卡圖雙圖層 (-ch/-bg) 製作與 Z-Order 調整**
  1. 將去背角色命名為 `12345-ch.png`，背景命名為 `12345-bg.png`，放入 `卡圖改/` 目錄。
  2. 模式選擇「超框卡圖後處理」，點擊 **[自動掃描目標目錄]**。
  3. 點選清單中的項目，展開預覽面板。
  4. 展開「進階雙圖層超框工坊」，調整角色縮放與平移，並拖曳 Z-Order 面板將「效果文字欄 (EffBox)」移至最底層隱藏。
  5. 展開「閃卡光澤與遮罩設定」，勾選外框與卡圖框的「透明化遮罩」，實現人物突破邊框的立體效果。
  6. 點擊 **[🚀 開始執行後處理]**，導出合成圖並自動跳轉 Tab 13。
* **SOP 8.3：Spine Cut-In 動畫綠幕去背與輸出**
  1. 模式選擇「卡片動畫後處理」。
  2. 將綠幕影片（如 `p18533.mp4`）放入 `卡圖改/`，點擊 **[自動掃描目標目錄]**。
  3. 點擊文字框內的 ID 自動帶入預覽，展開預覽面板。
  4. 勾選「啟用色彩去背」與「啟用防溢色修飾」，點擊 **[🎨 選擇去背色]** 吸取背景綠色。調整容錯度與溢色修正強度直至人物邊緣無綠光。
  5. 設定 4 點 POP UP 縮放曲線，點擊 **[生成全尺寸真實預覽 GIF]** 確認動態效果。
  6. 點擊 **[🚀 開始執行後處理]**，系統將執行 PMA 預乘並導出 `-hd.png`, `-hd.atlas.txt`, `js-hd.json` 以及對應的 SD 圖集至 `卡圖改/`。

---

### 10. 快捷單卡改圖 (Tab 9, `t9_quick_mod`)

<img width="1095" height="848" alt="單卡" src="https://github.com/user-attachments/assets/d357a412-561b-413a-9225-cfda5ad694aa" />

#### 技術機制與運作原理
跳過專案建置，直接針對單一檔案進行二進位注入。
* **大廳背景覆寫 (`MODE_LOBBY_BG`)**：精準鎖定 `masterduel_Data/data.unity3d` 中的 `ShopBGBase02` 貼圖，執行二進位注入並呼叫 `gc.collect()` 強制釋放記憶體。

#### 全介面元件與輸入框詳解
* **選擇分類 (ComboBox)**：「一般改圖」、「超框改圖」、「修改大廳背景」。
* **搜尋與選取區**：對照語系選單、搜尋框、僅處理場地/卡套開關、搜尋列表。
* **字典 (CSV) / 原檔來源 (0000) / 儲存根目錄 / 新圖片路徑 (Path Rows)**：讀寫路徑。
* **目標卡片 ID (LineEdit)**：欲替換之卡片 ID。
* **直接覆蓋遊戲檔案 (Checkbox)**：危險操作，直接修改 0000 原檔。

#### 按鈕功能詳解
* **[開始替換]**：執行單檔注入。
* **[傳送到超框改圖後處理]**：於超框模式下帶入路徑與 ID 跳轉至 Tab 8。

#### 本分頁獨立操作 SOP
* **SOP 9.1：快捷單卡替換**
  1. 分類選擇「一般改圖」。
  2. 於搜尋框尋找卡片，雙擊將 ID 帶入「目標卡片 ID」。
  3. 於「新圖片路徑」選擇替換圖片，點擊 **[開始替換]**。
* **SOP 9.2：大廳背景覆寫**
  1. 分類選擇「修改大廳背景」。
  2. 設定「原檔來源 (0000)」與「儲存根目錄」。
  3. 於「新圖片路徑」選擇高解析圖片。
  4. 點擊 **[開始替換]**。修改後的 `data.unity3d` 將生成於 `改完的文件/`。

---

### 11. 超框卡片註冊器 (Tab 13, `t13_overframe`)

<img width="1092" height="845" alt="超框" src="https://github.com/user-attachments/assets/900b1745-7aa6-4d1e-8a13-690644eacbf2" />

#### 技術機制與運作原理
讀寫解碼決定超框渲染之 `of_card_asset` 註冊表。
* **純淨基準檔 (.pristine)**：首次讀取時建立 `.pristine` 唯讀備份。未來寫入皆透過「UI 內容 - pristine 基準」計算出淨差異，再與最新官方資料進行合併與衝突判定。

#### 全介面元件與輸入框詳解
* **字典 (CSV) / 遊戲原檔來源 (0000) / 儲存根目錄 (Path Rows)**：路徑設定。
* **目標註冊表 (Hash) (ComboBox)**：當前 `of_card_asset` 之 Hash 碼。
* **欲合併用註冊表 (Path Row)**：供合併功能使用之外部註冊表。
* **現有白名單讀取與管理 (第一區塊文字框)**：展示遊戲內已存在的超框註冊資料。
* **新增註冊與寫入 (第二區塊文字框)**：供輸入欲新增之卡片 ID。支援直接貼上 Tab 2 萃取出來的格式，或手動輸入（語法格式：`觸發ID -> 目標底圖ID`。若無目標底圖，單填 `觸發ID` 即可。系統會自動忽略以 `#` 或 `=` 開頭的文字與註解）。

#### 按鈕功能詳解 (10 大按鈕)
1. **[🛠️ 手動爆搜修正 (大更新後專用)]**：掃描 0000 目錄尋找最新 `of_card_asset` 並自動轉移名單。
2. **[📥 讀取現有原檔]**：解碼二進位並顯示於第一區塊。
3. **[🧹 清除快取註冊表]**：刪除 `.pristine` 基準檔。
4. **[💾 儲存現有白名單]**：僅保存第一區塊的修改。
5. **[🔗 與「欲合併用註冊表」合併]**：合併外部註冊表並排除衝突。
6. **[🔍 前往「找出檔案」頁面挑選卡片]**：攜帶資料跳轉至 Tab 2。
7. **[📜 讀取過去成功註冊的歷史紀錄]**：載入過往註冊足跡至第二區塊。
8. **[✨ 正規化補名]**：反查 CSV 字典，自動補上中文名稱與對齊格式。
9. **[🧹 清除歷史紀錄]**：刪除 `registered_history.json`。
10. **[🚀 驗證衝突並輸出檔案]**：合併第一與第二區塊，寫入導出 Bundle。

#### 本分頁獨立操作 SOP
* **SOP 13.1：新增超框卡片註冊**
  1. 設定路徑，點擊 **[📥 讀取現有原檔]** 驗證第一區塊成功載入名單。
  2. 於第二區塊輸入 ID（或點擊 **[🔍 前往「找出檔案」頁面挑選卡片]** 跳轉挑選，或點擊 **[📜 讀取過去成功註冊的歷史紀錄]** 載入歷史）。
  3. 點擊 **[✨ 正規化補名]** 檢查名稱格式。
  4. 點擊 **[🚀 驗證衝突並輸出檔案]**。註冊表 Bundle 將生成於 `專案目錄/改完的文件/`。
  5. **實體覆寫關鍵步驟**：手動將 `改完的文件/` 內的 `of_card_asset` Bundle 複製貼回遊戲 0000 目錄（或透過 Tab 11 虛擬掛載）才會在遊戲內正式生效。
* **SOP 13.2：刪除白名單項目**
  1. 點擊 **[📥 讀取現有原檔]**。
  2. 直接於第一區塊文字框中手動刪除欲剔除的卡片行。
  3. 點擊 **[💾 儲存現有白名單]**（或 [🚀 驗證衝突並輸出檔案]），更新後的 Bundle 將生成於 `改完的文件/`。
  4. **實體覆寫關鍵步驟**：手動將該 Bundle 複製貼回遊戲 0000 目錄（或透過 Tab 11 虛擬掛載）。

---

### 12. 圖形化瀏覽器 (Tab 12, `t12_gallery`)

<img width="1053" height="837" alt="瀏覽" src="https://github.com/user-attachments/assets/2f553750-194c-4165-8ce7-420b45f93171" />

#### 技術機制與運作原理
提供 8 大非卡片資產之縮圖預覽與無痕沙盒過濾。
* **記憶體查表快取機制**：首次讀取時會提取 AssetBundle 中的 Texture2D 生成快取，後續讀取將跳過硬碟 I/O 直接從快取渲染，大幅提升檢視速度。
* **無慣性網格捲動**：針對網格清單導入線性捲動過濾器，實現網頁式的直上直下操作，徹底根除 Qt 內建捲動的震盪問題。
* **沙盒視覺化過濾 (`CAT_FILTER`)**：專屬的臨時生命週期容器。當從 Tab 2 發送批次 ID 至此時，系統會建立專屬的沙盒快取。使用者可在此進行圖形化的刪除操作，完成後將精簡過的名單無損回傳至 Tab 2。
* **雙擊快捷單卡映射**：於列表雙擊任一縮圖項目，系統會自動將該資產的真實 ID 發送至 Tab 9 (快捷單卡改圖) 並自動跳轉。

#### 全介面元件與按鈕功能
* **選擇分類 (ComboBox)**：提供場地、硬幣、卡盒、頭像框、頭像、卡套、大廳背景、檔案篩選 (沙盒模式) 8 種過濾維度。
* **[載入所選分類縮圖]**：掃描對照表並生成/讀取快取渲染網格。
* **[🧹 清除圖庫快取]**：安全刪除 `gallery_cache` 資料夾，釋放硬碟空間。
* **[傳送至「找出檔案」]**：將選取的項目及其關聯變體（例如：選取硬幣正面，系統會自動打包硬幣反面）送至 Tab 2 的提取清單。
* **[傳送至「快捷單卡改圖」]**：發送選取 ID 至 Tab 9。
* **[✨ 確認修改並返回]**：(僅於沙盒模式顯示) 帶回過濾後的精簡 ID 名單至 Tab 2 並自動銷毀沙盒。
* **快捷鍵操作**：選取項目後按 `Delete` 或 `Backspace` 鍵可刪除項目；按 `Ctrl+Z` 可復原刪除操作。

#### 本分頁獨立操作 SOP
* **SOP 12.1：資產檢視與雙擊快捷改圖**
  1. 選擇分類（如「頭像 (Icon)」），點擊 **[載入所選分類縮圖]**。
  2. 瀏覽網格尋找目標資產，**直接雙擊該縮圖**，系統會自動帶入 ID 並跳轉至 Tab 9 進入單卡替換流程。
* **SOP 12.2：Tab 2 聯動沙盒視覺化剔除**
  1. 於 Tab 2 載入大量 ID（或 TXT）後，點擊 **[🔍 視覺化篩選]** 跳轉至本頁的「檔案篩選」模式。
  2. 系統會自動生成清單中所有檔案的縮圖。
  3. 框選不合適或誤搜的縮圖，按 `Delete` 鍵將其剔除（若誤刪可按 `Ctrl+Z` 復原）。
  4. 點擊 **[✨ 確認修改並返回]**，系統會自動帶回精簡後的純淨 ID 名單至 Tab 2 並執行正規化對齊。
---

### 13. 模組串流修復器 (Tab 10, `t10_updater`)

<img width="1101" height="841" alt="修復" src="https://github.com/user-attachments/assets/6434cb46-c528-48d8-a799-486f0e45e16c" />

#### 技術機制與運作原理
解決遊戲大更新導致舊 Mod 黑圖或閃退之問題。
* **內嵌材質移植 (Inline Texture Injection)**：讀取「最新版官方原檔」，將其 `m_StreamData` 外部連結歸零，接著將舊 Mod 的二進位像素數據強行內嵌塞入新版 Bundle 中，完成無損升級。

#### 全介面元件與按鈕功能
* **1. 舊模組目錄 / 2. 遊戲原檔 (0000) / 3. 輸出根目錄 (Path Rows)**：路徑設定。
* **覆蓋舊模組 (Checkbox)**：危險操作，直接置換舊檔。
* **[開始串流修復]**：執行二進位移植。

#### 本分頁獨立操作 SOP：舊模組跨版本修復
1. 設定舊模組目錄與最新版 0000 原檔目錄。
2. 點擊 **[開始串流修復]**。修復後的檔案將導出至 `FIXED_MOD/` 資料夾。

---

### 14. 虛擬模組管理器 (Tab 11, `t11_virtual`)

<img width="1065" height="841" alt="虛擬" src="https://github.com/user-attachments/assets/53fa520d-d56b-43b5-ad74-b7a9a06cd143" />

#### 技術機制與運作原理
使用 Windows `os.symlink` (軟連結) 實現免複製、零硬碟佔用之模組秒級開關。
* **交易回滾 (Transaction Rollback)**：建立連結前將原檔重命名為 `.vanilla_backup` 並記錄於 `symlink_state.json`。若遭遇權限異常，瞬間觸發全局回滾還原原檔。
* **實時熱測試 (Hot-Reload) 核心優勢**：掛載軟連結後，軟連結會直接鎖定模組資料夾內的實體檔案。當遊戲開啟時，若您修改並重新輸出打包資源到該模組資料夾中，**遊戲內再次播放或載入該資產時會直接實時讀取到最新資料，完全不需要重新連結、不需要複製貼上檔案，更不需要重新啟動遊戲**。

#### 全介面元件與按鈕功能
* **模組存放根目錄 / 遊戲 0000 資料夾 (Path Rows)**：路徑設定。
* **掃描深度 (ComboBox)**：穿透尋找 `0000` 之最大層級 (1~5)。
* **啟用與停用清單 (ListWidgets)**：左側為啟用，右側為停用。
* **[重新整理]**：重新掃描模組根目錄。
* **[>>] / [<<]**：全停用 / 全啟用選取項目。
* **[▲] / [▼]**：調整啟用優先權（上方覆蓋下方）。
* **[▶ 套用並儲存]**：執行軟連結掛載與備份。

#### 本分頁獨立操作 SOP：虛擬掛載與實時熱測試
1. 將多個模組資料夾放入「模組存放根目錄」。
2. 點擊 **[重新整理]**，雙擊項目將欲啟用的模組移至左側清單。
3. 點擊 **[▶ 套用並儲存]**（需管理員權限）。
4. **實時熱測試**：保持遊戲開啟，若於 Tab 8 或 Tab 4 重新輸出打包了新 Bundle 至該模組資料夾，遊戲內再次觸發該資產時即可實時看到最新效果，無需重開遊戲。

---

## 第三章：全場景實戰案例庫（端到端流程組合）

本章節提供跨分頁的組合流程 SOP。

### 案例一：從頭開始製作標準卡圖替換模組
1. **Tab 1 (爆搜)**：設定 0000 目錄，點擊 **[開始掃描]** 生成 `2DTexture_Mapping.csv`。
2. **Tab 2 (找出)**：輸入卡片名稱或 ID，加入列表，點擊 **[開始提取原檔]** 複製出二進位檔案至 `原檔/`。
3. **Tab 3 (提取)**：點擊 **[開始提取內容物]**，解包導出可編輯 PNG 至 `原卡圖/`。
4. **繪圖修改**：使用圖像軟體修改 PNG，存入 `卡圖改/`。
5. **Tab 4 (改圖)**：點擊 **[開始替換]**，將新圖片寫回 Bundle，導出至 `改完的文件/`。
6. **Tab 5 (封裝)**：勾選打包選項，點擊 **[開始打包]** 生成完整模組壓縮檔。

### 案例二：將普通卡片製作為全新超框卡片
1. **Tab 1 -> Tab 2 -> Tab 3**：按案例一流程提取出卡片原圖。
2. **Tab 8 (後處理)**：選擇「超框卡圖後處理」，調整 PSD 5 圖層透明度，點擊 **[🚀 開始執行後處理]**，生成 11:16 超框合成圖至 `卡圖改/`。
3. **Tab 4 (改圖)**：點擊 **[開始替換]**，將超框圖片寫回 Bundle，導出至 `改完的文件/`。
4. **Tab 13 (註冊器)**：點擊 **[📥 讀取現有原檔]**，於第二區塊輸入該卡片 ID，點擊 **[✨ 正規化補名]**，點擊 **[🚀 驗證衝突並輸出檔案]** 導出 `of_card_asset` 至 `改完的文件/`。
5. **實體覆寫**：將 `改完的文件/` 內的所有 Bundle 複製貼回遊戲 0000 目錄（或使用 Tab 11 掛載）。

### 案例三：從零製作並替換卡片登場動畫 (Spine Cut-In)
1. 準備一張靜態圖片或綠幕影片或是準備連續的圖片裝在PSD或資料夾中，命名為 `p18533.png` 或 `p18533.mp4`... 存入 `卡圖改/`。
2. **Tab 8 (後處理)**：選擇「卡片動畫後處理」，選擇去背顏色並調整 Despill 溢色修正與 POP UP 曲線，點擊 **[🚀 開始執行後處理]** 生成 Spine 圖集。
3. **Tab 4 (改圖)**：點擊 **[開始替換]**，寫回動畫 Bundle，工具自動校正 Gamma 色彩空間。
4. **Tab 5 (封裝)**：打包發布。

### 案例四：修復遊戲大更新後失效的超框註冊表
1. 遊戲大更新後，進入 **Tab 13 (註冊器)**。
2. 點擊 **[🛠️ 手動爆搜修正 (大更新後專用)]**。系統自動掃描最新 0000 目錄，尋找最新 Hash 並將歷史註冊名單無縫轉移。
3. 點擊 **[🚀 驗證衝突並輸出檔案]** 生成最新註冊表 Bundle。
4. 將導出之 Bundle 複製貼回遊戲 0000 目錄。

### 案例五：舊版本失效模組救援與熱測試
1. **Tab 10 (修復器)**：設定舊 Mod 目錄與最新 0000 目錄，點擊 **[開始串流修復]** 導出修復 Bundle。
2. **Tab 11 (虛擬掛載)**：將修復後的模組放入模組根目錄，點擊 **[▶ 套用並儲存]**。直接開啟遊戲驗證，無需重開遊戲即可實時預覽。

---

## 第四章：工程進階技巧指南 (Advanced Techniques)

### 進階技巧一：逆向掃描「改完的文件」快速導出修改清單 (TXT)
* **情境**：當取得一個現成的模組包或 `改完的文件/` 資料夾，欲快速獲取其中包含的所有卡片 ID 以帶入 Tab 13 註冊超框或 Tab 2 對照時。
* **操作 SOP**：
  1. 進入 **Tab 1 (爆搜與補齊)**。
  2. 將「目標資料夾 (0000)」路徑**手動指向「改完的文件」或模組資料夾**。
  3. 僅勾選「產生內容物列表(TXT)」，取消勾選「產生對照表(CSV)」以節省時間。
  4. 點擊 **[開始掃描]**。
  5. 系統會讀取該資料夾內的所有 Bundle，反向萃取出所有貼圖與資產 ID，並於輸出目錄生成 `Extracted_Names.txt`。
  6. 此 TXT 可直接載入 Tab 2 或貼入 Tab 13，免去手動逐一輸入的麻煩。

### 進階技巧二：利用虛擬掛載 (Tab 11) 進行極速動畫/卡圖免重開遊戲熱測試
* 掛載軟連結後，當遊戲處於開啟狀態時，若重新輸出打包資源至模組資料夾中，遊戲內再次播放或載入該資產時會直接實時讀取新資料，無需重新連結、無需複製貼上，更無需重開遊戲。

### 進階技巧三：Tab 2 與 Tab 12 沙盒視覺化剔除雙向流轉
* 於 Tab 2 輸入批次 ID 後點擊 **[搜尋 / 視覺化篩選]** 跳轉 Tab 12，按 `Delete` 剔除不符項，點擊 **[✨ 確認修改並返回]** 自動帶回精簡後的 ID 名單，或是你可以點兩下想選取的物件將其傳送至快捷單卡改圖。

### 進階技巧四：快捷鍵與路徑空白鍵強制刷新秘技
* 當 Tab 2 的路徑連動未即時更新至 Tab 3 時，於 Tab 2 的路徑輸入框中間**點擊按一下空白鍵再刪除該空白鍵**，即可強制觸發 `textChanged` 信號完成全頁面路徑刷新。

---

## 第五章：系統異常排除指南 (Troubleshooting)

1. **`PermissionError: 權限不足`**
   * **原因**：`2DTexture_Mapping.csv` 正在被 Microsoft Excel 或其他軟體開啟，導致 Python 無法取得寫入鎖 (Write Lock)。
   * **排除**：關閉 Excel 或相關編輯器後重新執行。

2. **記憶體溢出 (OOM) 或介面凍結**
   * **排除**：至 **Tab 7 (設定與外觀)** 將「最大算力(執行緒)」由 `Auto` 調降為 2 或 4。處理動畫時勾選「啟用硬碟暫存 (防記憶體溢出)」。

3. **`.pristine` 基準檔保護與還原**
   * **警告**：請勿隨意刪除 `MD_Tool_Temp/of_card_asset_backups/` 內的 `.pristine` 檔案。若基準檔遺失且發生異常，可點擊 Tab 13 的 **[🛠️ 手動爆搜修正]** 重新建立最新基準。